黑色遮罩 效果里最后两个
暂时无用 因为会导致背景素材变模糊
```js
'use strict';

function mapMousePosition(mouseX) {
    const minInput = 0;
    const maxInput = 3440;
    const minOutput = 0.3;
    const maxOutput = 0.7;
    // 线性映射公式
    return minOutput + (mouseX - minInput) * (maxOutput - minOutput) / (maxInput - minInput);

}

export function update(value) {
    // var xzhou = input.cursorWorldPosition;
    // console.log(input.cursorWorldPosition.x)

    const mouseX = input.cursorWorldPosition.x;
    // 映射鼠标位置
    value = mapMousePosition(mouseX);
    // console.log(value)
    // 输出映射后的值，这里可以根据实际需求实现动画效果
    // 持续更新，模拟动画
    // requestAnimationFrame(mouseMoveAnimation);
    return value;
}
```

```js
'use strict';

function mapMousePosition(mouseX) {
    const minInput = 0;
    const maxInput = 1440;
    const minOutput = 0.6;
    const maxOutput = 0.4;
    // 线性映射公式
    return minOutput + (mouseX - minInput) * (maxOutput - minOutput) / (maxInput - minInput);
}

export function update(value) {
    // var xzhou = input.cursorWorldPosition;
    // console.log(input.cursorWorldPosition.x)
    const mouseX = input.cursorWorldPosition.y;
    // 映射鼠标位置
    value = mapMousePosition(mouseX);
    // console.log(value)
    // 输出映射后的值，这里可以根据实际需求实现动画效果
    // 持续更新，模拟动画
    // requestAnimationFrame(mouseMoveAnimation);
    return value;
}
```