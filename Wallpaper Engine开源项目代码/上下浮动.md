
```js
'use strict';

export function update(value) {
	value.y = 500 + (engine.runtime * 100);
	return value;
}
```

```js
'use strict';

export function update(value) {
	value.y = 500 + (Math.sin(engine.runtime) * 100);
	return value;
}
```
